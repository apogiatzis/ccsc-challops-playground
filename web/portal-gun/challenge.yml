name: "Portal Gun"
author: "rok0s"
category: web

description: |
  Don't look at another man's portal gun history. We all go to weird places.

value: 500
type: dynamic_docker
extra:
  initial: 500
  minimum: 100
  decay: 50
  compose_stack: !filecontents docker-compose.yml

flags:
  - CCSC{P0r74L_t0_noSqL_d1MenS10n!!}

tags:
  - web

files:
  - public/portal-gun.tar.gz

state: visible
version: "0.1"